<tr>
  <td><%= category.name %></td>
  <td><%= link_to t("actions.edit"), edit_category_path(category), :class => "btn btn-primary" %></td>
  <td><%= link_to t("actions.delete"), category, confirm: 'Are you sure?', method: :delete, :class => "btn btn-danger" %></td>
</tr>
